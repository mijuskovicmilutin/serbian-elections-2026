package rs.serbianelection2026.backend.survey.entity;

/** What a question is for. The demographic roles are the raking dimensions. */
public enum SurveyRole {
    VOTE_INTENTION,
    TURNOUT,
    AGE,
    SEX,
    REGION,
    SETTLEMENT
}

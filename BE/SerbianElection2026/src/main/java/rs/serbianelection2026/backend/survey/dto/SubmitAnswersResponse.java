package rs.serbianelection2026.backend.survey.dto;

/** Deliberately says nothing else: no id, no count, nothing that could identify the response. */
public record SubmitAnswersResponse(boolean accepted) {
}

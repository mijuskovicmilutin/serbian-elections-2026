/** News RSS ingestion pipeline: parse (RssFeedParser), per-source fetch (NewsProvider implementations), normalize/save (NewsImportService), schedule (NewsImportJob). */
package rs.serbianelection2026.backend.ingestion.news;
